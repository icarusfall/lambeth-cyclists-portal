# Web
fastapi>=0.141,<1
uvicorn[standard]>=0.52,<1
jinja2>=3.1,<4
python-multipart>=0.0.32
itsdangerous>=2.2,<3

# Auth — hashpw/checkpw/gensalt only; existing $2b$ hashes verify unchanged.
bcrypt>=5,<6

# Config
pydantic-settings>=2.15,<3

# Notion — v3 data_sources API.
notion-client>=3,<4

# Claude API — 1.x. Uses messages.parse(output_format=Model), output_config
# effort, and the MCP connector beta; no sampling params (removed in 1.x).
anthropic>=1,<2

# Sending / rendering
resend>=2.42,<3
markdown>=3.10,<4
